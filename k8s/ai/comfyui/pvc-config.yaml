apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comfyui-config-pvc
  namespace: ai
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-zfs-kubedata-ext4
  resources:
    requests:
      storage: 1Gi
