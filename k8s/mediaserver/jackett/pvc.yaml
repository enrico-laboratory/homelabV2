apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jackett-pvc-pve
  namespace: mediaserver
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-zfs-nvme
  resources:
    requests:
      storage: 50Mi
