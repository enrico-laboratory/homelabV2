apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-pvc-pve
  namespace: mediaserver
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-zfs-nvme
  resources:
    requests:
      storage: 700Mi
